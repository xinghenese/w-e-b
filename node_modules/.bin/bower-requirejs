#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../bower-requirejs/bin/bower-requirejs.js" "$@"
  ret=$?
else 
  node  "$basedir/../bower-requirejs/bin/bower-requirejs.js" "$@"
  ret=$?
fi
exit $ret
